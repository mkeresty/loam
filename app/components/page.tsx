import Link from 'next/link'
import { components } from '../../lib/components'
import { ThemeControl } from './theme-control'
export default function ComponentsPage(){return <main><nav className="nav shell"><Link className="brand" href="/"><span className="brand-flower">✤</span>LOAM</Link><div className="docs-nav-actions"><Link href="/">Back to field library</Link><ThemeControl/></div></nav><section className="docs-index shell"><span className="kicker">COMPONENT INDEX / {String(components.length).padStart(2,'0')}</span><h1>Every object,<br/><em>in the open.</em></h1><p className="docs-lead">Twenty installable pieces, each documented with a working specimen and the rules that shape it.</p><div className="docs-list">{components.map(([slug,name,description],i)=><Link href={`/components/${slug}`} key={slug}><span>{String(i+1).padStart(2,'0')}</span><strong>{name}</strong><p>{description}</p><b>↗</b></Link>)}</div></section></main>}
