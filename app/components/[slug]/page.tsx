import { notFound } from 'next/navigation'
import Link from 'next/link'
import { components,componentBySlug } from '../../../lib/components'
import { ComponentPreview } from '../preview'
export function generateStaticParams(){return components.map(([slug])=>({slug}))}
export default async function ComponentPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const item=componentBySlug(slug);if(!item)notFound();const[,name,description]=item;const command=`npx shadcn@latest add https://loam-ui.kerestymason.chatgpt.site/r/${slug}.json`;return <main><nav className="nav shell"><Link className="brand" href="/"><span className="brand-flower">✤</span>LOAM</Link><Link href="/components">All components</Link></nav><article className="docs-detail shell"><header><span className="kicker">COMPONENT / {slug.toUpperCase()}</span><h1>{name}</h1><p>{description} Own the source, adjust the details, and keep the interaction legible.</p></header><section className="preview-frame"><div className="preview-label"><span>INTERACTIVE SPECIMEN</span><span>LIGHT / DARK READY</span></div><ComponentPreview slug={slug}/></section><div className="docs-columns"><section><span className="kicker">INSTALL</span><code>{command}</code></section><section><span className="kicker">MOTION</span><p>Motion describes state, respects reduced-motion preferences, and never changes the control’s footprint.</p></section><section><span className="kicker">ACCESSIBILITY</span><p>Built on semantic controls or Radix primitives with keyboard behavior, focus management, and clear state.</p></section><section><span className="kicker">VARIANTS</span><p>Default is flat. Quiet removes fill. Seasonal adds one contextual accent. Elevated depth is reserved for overlays.</p></section></div></article></main>}
